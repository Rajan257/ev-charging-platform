# Production environment - Vercel deployment
# API calls use relative paths (/api/v1/*) which Vercel rewrites
# to the backend on Render via vercel.json rewrites.
VITE_API_BASE_URL=/api/v1
VITE_BACKEND_URL=https://ev-charging-platform-iz4z.onrender.com
